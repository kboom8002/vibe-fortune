"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Slider } from "@/components/ui/slider";
import { Sparkles, Brain, Flame, Activity, Zap, Heart, AlertTriangle, ShieldCheck } from "lucide-react";
import { Navbar } from "@/components/layout/Navbar";

export default function DailyLoopPage() {
  const router = useRouter();
  const [profile, setProfile] = useState<{ name?: string } | null>(null);

  // Vibe state scores (0 to 10, default to neutral 5)
  const [valence, setValence] = useState(5);
  const [arousal, setArousal] = useState(5);
  const [energy, setEnergy] = useState(5);
  const [focus, setFocus] = useState(5);
  const [socialLoad, setSocialLoad] = useState(5);
  const [oneLineEvent, setOneLineEvent] = useState("");

  const [currentFocus, setCurrentFocus] = useState("business_finance");
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState("");

  useEffect(() => {
    // Check if birth profile exists; if not, redirect to onboarding
    const storedProfile = localStorage.getItem("user-birth-profile");
    if (!storedProfile) {
      router.push("/app/onboarding");
      return;
    }
    setProfile(JSON.parse(storedProfile));
  }, [router]);

  const handleGenerate = async (e: React.FormEvent) => {
    e.preventDefault();
    setLoading(true);
    setError("");

    try {
      // Save Vibe check-in
      const vibeCheckIn = {
        id: crypto.randomUUID(),
        userId: "local-user",
        valence,
        arousal,
        energy,
        focus,
        socialLoad,
        oneLineEvent,
        createdAt: new Date().toISOString(),
      };

      localStorage.setItem("user-vibe-checkin", JSON.stringify(vibeCheckIn));

      // Call API or Mock LangGraph node trigger
      // We will implement Phase 4 Agent Graph shortly.
      // For now, save forecast request metadata and redirect to result
      const forecastRequestId = crypto.randomUUID();
      localStorage.setItem(
        "last-forecast-request",
        JSON.stringify({
          id: forecastRequestId,
          currentFocus,
          targetDate: new Date().toISOString().split("T")[0],
        })
      );

      // We'll generate a mock result or actual forecast result
      router.push(`/app/result/${forecastRequestId}`);
    } catch (err: any) {
      setError(err.message || "遺꾩꽍 ?占쎌껌 占??占쎈쪟媛 諛쒖깮?占쎌뒿?占쎈떎.");
    } finally {
      setLoading(false);
    }
  };

  const focusDomains = [
    { value: "business_finance", label: "鍮꾩쫰?占쎌뒪 & 湲덉쑖", icon: Flame, color: "text-amber-400" },
    { value: "relationship_love", label: "?占?占쏙옙?占?& ?占쎌븷", icon: Heart, color: "text-pink-400" },
    { value: "health_recovery", label: "嫄닿컯 & 由ъ빱踰꾨━", icon: Activity, color: "text-emerald-400" },
    { value: "learning_writing_research", label: "?占쎌뒿 & ?占쎄뎄 & ?占??, icon: Brain, color: "text-sky-400" },
    { value: "reputation_branding", label: "紐낆꽦 & ?占쎌뒪??釉뚮옖??, icon: Sparkles, color: "text-purple-400" },
    { value: "risk_legal_safety", label: "?占쏀뿕 愿占?& 踰뺣쪧", icon: AlertTriangle, color: "text-red-400" },
  ];

  return (
    <div className="min-h-screen bg-zinc-950 text-zinc-50 flex flex-col justify-between relative overflow-hidden font-sans">
      <div className="absolute top-[-10%] left-[-10%] w-[50%] h-[50%] rounded-full bg-indigo-900/10 blur-[120px] pointer-events-none" />
      <div className="absolute bottom-[-10%] right-[-10%] w-[50%] h-[50%] rounded-full bg-purple-900/10 blur-[120px] pointer-events-none" />

      <Navbar />

      <div className="max-w-xl w-full mx-auto py-12 px-6 space-y-8 relative z-10 flex-1 flex flex-col justify-center">
        {/* Title */}
        <div className="text-center space-y-2">
          <div className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-zinc-900 border border-zinc-800 text-zinc-400 text-xs font-mono">
            <ShieldCheck className="w-3.5 h-3.5 text-indigo-400" />
            Vibe Check-in
          </div>
          <h1 className="text-3xl font-bold tracking-tight">
            ?占쎈뒛, ?占쎌떊??諛붿씠釉뚮뒗 ?占쎈뼡媛??
          </h1>
          <p className="text-sm text-zinc-400 max-w-sm mx-auto leading-relaxed">
            {profile?.name || "?占쎌슜??}?占쎌쓽 ?占쎌옱 紐멸낵 留덉쓬 ?占쏀깭占?湲곕줉??二쇱떆占??占쎌＜ 援ъ“(Prior)?占?寃고빀?占쎌뿬 留욎땄??泥섎갑???占쎌텧?占쎈땲??
          </p>
        </div>

        {error && (
          <div className="p-3 bg-red-950/40 border border-red-900/50 rounded-xl text-xs text-red-400 text-center">
            {error}
          </div>
        )}

        <form onSubmit={handleGenerate} className="space-y-8 bg-zinc-900/30 border border-zinc-800/80 p-8 rounded-3xl backdrop-blur-md">
          {/* Vibe Sliders */}
          <div className="space-y-6">
            <h3 className="text-sm font-semibold text-zinc-300 uppercase tracking-wider border-b border-zinc-800/80 pb-3 flex items-center gap-2">
              <Zap className="w-4 h-4 text-indigo-400" />
              ?占쎌떆占??占쏀깭 吏꾨떒
            </h3>

            {/* Valence */}
            <div className="space-y-2">
              <div className="flex justify-between text-xs font-medium">
                <span className="text-zinc-400">湲곕텇 (Valence)</span>
                <span className="text-indigo-400 font-mono">{valence} / 10</span>
              </div>
              <Slider
                value={[valence]}
                onValueChange={(val) => setValence(Array.isArray(val) ? val[0] : val)}
                max={10}
                step={1}
                className="py-2"
              />
              <div className="flex justify-between text-[10px] text-zinc-600">
                <span>留ㅼ슦 移⑥슱??/ ?占쎈쭩</span>
                <span>留ㅼ슦 ?占쎈났 / 湲띿젙</span>
              </div>
            </div>

            {/* Arousal */}
            <div className="space-y-2">
              <div className="flex justify-between text-xs font-medium">
                <span className="text-zinc-400">媛곸꽦??(Arousal)</span>
                <span className="text-indigo-400 font-mono">{arousal} / 10</span>
              </div>
              <Slider
                value={[arousal]}
                onValueChange={(val) => setArousal(Array.isArray(val) ? val[0] : val)}
                max={10}
                step={1}
                className="py-2"
              />
              <div className="flex justify-between text-[10px] text-zinc-600">
                <span>臾닿린??/ 議몃┝</span>
                <span>留ㅼ슦 湲댁옣 / ?占쎈텇</span>
              </div>
            </div>

            {/* Energy */}
            <div className="space-y-2">
              <div className="flex justify-between text-xs font-medium">
                <span className="text-zinc-400">?占쎈젰 (Energy)</span>
                <span className="text-indigo-400 font-mono">{energy} / 10</span>
              </div>
              <Slider
                value={[energy]}
                onValueChange={(val) => setEnergy(Array.isArray(val) ? val[0] : val)}
                max={10}
                step={1}
                className="py-2"
              />
              <div className="flex justify-between text-[10px] text-zinc-600">
                <span>諛⑹쟾??/ 洹뱀떖???占쎈줈</span>
                <span>?占쎈꼫吏 ?占쎌묠</span>
              </div>
            </div>

            {/* Focus */}
            <div className="space-y-2">
              <div className="flex justify-between text-xs font-medium">
                <span className="text-zinc-400">吏묒쨷??(Focus)</span>
                <span className="text-indigo-400 font-mono">{focus} / 10</span>
              </div>
              <Slider
                value={[focus]}
                onValueChange={(val) => setFocus(Array.isArray(val) ? val[0] : val)}
                max={10}
                step={1}
                className="py-2"
              />
              <div className="flex justify-between text-[10px] text-zinc-600">
                <span>?占쎈쭔??/ 釉뚮젅???占쎄렇</span>
                <span>怨좊룄??紐곗엯 ?占쏀깭</span>
              </div>
            </div>

            {/* Social Load */}
            <div className="space-y-2">
              <div className="flex justify-between text-xs font-medium">
                <span className="text-zinc-400">愿占?遺??(Social Load)</span>
                <span className="text-indigo-400 font-mono">{socialLoad} / 10</span>
              </div>
              <Slider
                value={[socialLoad]}
                onValueChange={(val) => setSocialLoad(Array.isArray(val) ? val[0] : val)}
                max={10}
                step={1}
                className="py-2"
              />
              <div className="flex justify-between text-[10px] text-zinc-600">
                <span>?占쎌옄 ?占쎄퀬 ?占쎌쓬</span>
                <span>?占쎄레???占쏀넻 媛??/span>
              </div>
            </div>
          </div>

          {/* Forecast Focus */}
          <div className="space-y-4 pt-4">
            <h3 className="text-sm font-semibold text-zinc-300 uppercase tracking-wider border-b border-zinc-800/80 pb-3 flex items-center gap-2">
              <Brain className="w-4 h-4 text-indigo-400" />
              ?占쎌쁺 蹂대뱶 吏묒쨷 遺꾩빞
            </h3>

            <div className="grid grid-cols-2 gap-3">
              {focusDomains.map((domain) => {
                const IconComponent = domain.icon;
                const isSelected = currentFocus === domain.value;
                return (
                  <button
                    key={domain.value}
                    type="button"
                    onClick={() => setCurrentFocus(domain.value)}
                    className={`p-4 rounded-2xl border text-left flex flex-col justify-between h-24 transition-all ${
                      isSelected
                        ? "border-indigo-500 bg-indigo-950/20 text-white shadow-lg shadow-indigo-500/5"
                        : "border-zinc-800 bg-zinc-950/40 text-zinc-400 hover:border-zinc-700/60"
                    }`}
                  >
                    <IconComponent className={`w-5 h-5 ${domain.color}`} />
                    <span className="text-xs font-semibold">{domain.label}</span>
                  </button>
                );
              })}
            </div>
          </div>

          {/* One line event */}
          <div className="space-y-2 pt-2">
            <label className="block text-xs font-semibold text-zinc-400 uppercase tracking-wider mb-2">
              ?占쎈뒛??二쇱슂 ?占쎈깽??/ ??占?留λ씫 (?占쏀깮)
            </label>
            <input
              type="text"
              value={oneLineEvent}
              onChange={(e) => setOneLineEvent(e.target.value)}
              placeholder="?? 以묒슂???占쎈큺 ?占쎌긽 ?占쎌젙, ?占쏀듃?占쏙옙? ?占쏀댘???占쎌뿀????
              className="w-full bg-zinc-950 border border-zinc-800 rounded-xl px-4 py-3 text-sm focus:outline-none focus:border-indigo-500 transition-colors placeholder:text-zinc-600"
            />
          </div>

          {/* Submit */}
          <Button
            type="submit"
            disabled={loading}
            className="w-full bg-gradient-to-r from-indigo-500 to-purple-600 hover:from-indigo-600 hover:to-purple-700 text-white font-medium py-4 rounded-xl shadow-lg shadow-indigo-500/10 border-0 transition-transform active:scale-[0.98] flex items-center justify-center gap-2"
          >
            <Sparkles className="w-4 h-4" />
            {loading ? "?占쎌쁺 蹂대뱶 ?占쎌궛 占?.." : "?占쎈뒛???占쎄린?占쎌쁺 蹂대뱶 ?占쎌텧"}
          </Button>
        </form>
      </div>
    </div>
  );
}

